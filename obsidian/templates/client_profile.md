---
title: {{ client.name }}
client_id: {{ client.id }}
industry: {{ client.industry or client.business_type }}
city: {{ client.city }}
state: {{ client.state }}
website: {{ client.website }}
primary_keyword: {{ client.primary_keyword }}
active: {{ client.active }}
tags: [client, {{ client.id }}{% for tag in client.tags %}, {{ tag }}{% endfor %}]
created: {{ date }}
updated: {{ date }}
---

# {{ client.name }}

> {{ client.industry or client.business_type }} · {{ client.city }}, {{ client.state }}

## Quick Reference

| Field | Value |
|-------|-------|
| Website | {{ client.website or "—" }} |
| Primary Keyword | {{ client.primary_keyword or "—" }} |
| GBP Profile | {{ client.gbp.profile_url or "—" }} |
| GBP Category | {{ client.gbp.primary_category or "—" }} |
| GBP Place ID | {{ client.gbp.place_id or "—" }} |
| Local Falcon IDs | {{ client.local_falcon.location_ids | join(', ') or "—" }} |
| Local Falcon Grid | {{ client.local_falcon.default_grid }} @ {{ client.local_falcon.default_radius_miles }}mi |
| Yext Account | {{ client.yext.account_id or "—" }} |
| BrightLocal | {{ client.yext.brightlocal_id or "—" }} |
| Plan | {{ client.billing.plan or "—" }} (${{ client.billing.monthly_usd }}/mo) |

## Contact

| Field | Value |
|-------|-------|
| Name | {{ client.contact.name or "—" }} |
| Email | {{ client.contact.email or "—" }} |
| Phone | {{ client.contact.phone or "—" }} |

## Keywords

**Primary:** {{ client.primary_keyword or "—" }}

**Secondary:**
{% for kw in client.secondary_keywords %}- {{ kw }}
{% else %}- (none set)
{% endfor %}

## Citation Targets

{% for dir in client.citations.target_directories %}- {{ dir }}
{% else %}- (none configured)
{% endfor %}
Last audit: {{ client.citations.last_audit or "never" }}

## Per-Client Overrides

```json
{{ client.overrides | tojson(indent=2) }}
```

## Notes

{{ client.notes or "No notes yet." }}

---

## Linked Resources

```dataview
LIST
FROM "1-Projects/{{ client.id }}" OR "3-Resources/Research"
WHERE client = "{{ client.id }}"
SORT file.mtime DESC
LIMIT 20
```

## Skill Status

```dataview
TABLE date, file.name AS "Report"
FROM "1-Projects/{{ client.id }}"
SORT date DESC
LIMIT 10
```

---
*Profile auto-generated by Jarvis-Pro. Edit freely — body edits are preserved on re-sync.*
